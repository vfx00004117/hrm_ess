import { View, Text } from "react-native";

export default function RequestsScreen() {
    return (
        <View className="flex-1 items-center justify-center">
            <Text className="text-xl font-semibold">Requests (manager)</Text>
        </View>
    );
}
